#you have two classes, takes string name and int price, class name is Item, 
# #second class is shopping cart, woth three methods, add method, takes item as input,
# second method is len takes number of items in the cart,
# total is the third emthod returns total price

class Item():
    def __init__(self, name, price):
        self.name = name
        self.age = price
    
class Cart():
    def add(item:Item):
        ilist.append(item)
        print("Added")
    
    def leng():
        print("total items : "+len(list))